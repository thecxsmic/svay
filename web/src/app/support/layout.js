export const metadata = {
  title: "Support",
  description: "Svay customer care — billing help, account issues, and contact",
};

export default function SupportLayout({ children }) {
  return children;
}
