export const metadata = {
  title: "Free YouTube Tools",
  description:
    "Free public creator tools from Svay — earnings calculator, title analyzer, tag generator, and virality score estimator. No account required.",
};

export default function ToolsLayout({ children }) {
  return children;
}
